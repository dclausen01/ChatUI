# Environment Variables for ChatUI Backend

# Port for the backend server (optional, defaults to 3001)
PORT=3001

# Encryption key for API keys (IMPORTANT: Change this in production!)
# Generate a strong random key for production use
ENCRYPTION_KEY=your-secure-encryption-key-here-change-in-production

# Example of generating a secure key:
# You can generate a secure key using Node.js:
# require('crypto').randomBytes(32).toString('hex')
